% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxa_compose.R
\name{ap_top_taxa}
\alias{ap_top_taxa}
\title{Most abundant taxa}
\usage{
ap_top_taxa(x, n = 15L, group = NULL, rank = NULL)
}
\arguments{
\item{x}{A \code{TreeSummarizedExperiment}, usually from \code{\link[=ap_collapse]{ap_collapse()}}.}

\item{n}{Number of taxa to keep. Default \code{15}.}

\item{group}{Optional metadata variable. When given, a taxon qualifies if it
makes the top \code{n} in any group, so a taxon abundant in a small group is
not hidden by a large one.}

\item{rank}{Rank to collapse to first, when \code{x} has not been collapsed.}
}
\value{
A data frame: \code{sample_id}, \code{taxon}, \code{relative_abundance}, and \code{group}
when given. \code{taxon} is a factor ordered by overall abundance with \code{Other}
last.
}
\description{
Ranks taxa by mean relative abundance across samples and pools the rest as
\code{Other}.
}
\details{
Mean relative abundance, not total counts. Ranking on totals lets a handful
of deeply sequenced samples decide which taxa appear, which is a property of
the library preparation rather than of the biology.
}
