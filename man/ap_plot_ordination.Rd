% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_beta.R
\name{ap_plot_ordination}
\alias{ap_plot_ordination}
\title{Plot an ordination}
\usage{
ap_plot_ordination(
  ord,
  group = NULL,
  shape = NULL,
  permanova = NULL,
  ellipse = TRUE,
  axes = c(1, 2),
  point_size = 1.6,
  publication = FALSE,
  pub = ap_pub_options()
)
}
\arguments{
\item{ord}{An \code{ap_ordination} from \code{\link[=ap_ordinate]{ap_ordinate()}}.}

\item{group}{Metadata variable to colour by.}

\item{shape}{Optional second metadata variable mapped to point shape.}

\item{permanova}{Optional \code{ap_permanova} result to annotate with.}

\item{ellipse}{Draw a 95\% confidence ellipse per group. Default \code{TRUE}.}

\item{axes}{Which two axes to plot. Default \code{c(1, 2)}.}

\item{point_size}{Point size. Default \code{1.6}.}

\item{publication}{Draw the publication version: no subtitle or caption,
\code{\link[=ap_theme_pub]{ap_theme_pub()}}, ggsci colours, ellipses filled at low opacity, and the
PERMANOVA and dispersion numbers on the panel without the verdict. The
verdict, its explanation and the projection caveat go to the legend text
from \code{\link[=ap_ordination_legend]{ap_ordination_legend()}}. Default \code{FALSE}.}

\item{pub}{\code{\link[=ap_pub_options]{ap_pub_options()}} for \code{publication = TRUE}.}
}
\value{
A ggplot object.
}
\description{
PCoA or NMDS with the numbers that make it readable: variance explained on
the axes, stress in the corner for NMDS, and the PERMANOVA result with its
dispersion verdict written onto the panel.
}
\details{
The dispersion verdict is on the figure rather than in the caption on
purpose. An ellipse plot with \code{p = 0.001} reads as "these groups are
different"; if the dispersion test also fired, that reading is wrong, and the
figure is where the reader will form it.
}
