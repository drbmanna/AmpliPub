% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{ap_normalize}
\alias{ap_normalize}
\title{Normalize a count table}
\usage{
ap_normalize(
  x,
  method = c("tss", "css", "clr", "rarefy"),
  depth = NULL,
  seed = 1L,
  pseudocount = 0.5,
  css_p = NULL,
  css_scale = 1000
)
}
\arguments{
\item{x}{A \code{TreeSummarizedExperiment} from \code{\link[=ap_import]{ap_import()}}.}

\item{method}{One of \code{"tss"}, \code{"css"}, \code{"clr"}, \code{"rarefy"}.}

\item{depth}{Rarefaction depth for \code{"rarefy"}. \code{NULL} uses the smallest library.}

\item{seed}{Random seed for \code{"rarefy"}.}

\item{pseudocount}{Value substituted for zeros before the CLR. Default \code{0.5}.}

\item{css_p}{Quantile for CSS. \code{NULL} lets \code{metagenomeSeq::cumNormStatFast} choose it.}

\item{css_scale}{The \code{sl} scaling passed to \code{metagenomeSeq::MRcounts}. Default \code{1000},
metagenomeSeq's own default, passed explicitly.}
}
\value{
A features-by-samples numeric matrix, with the settings used in its attributes
(\code{method}, and \code{css_p}, \code{depth} or \code{pseudocount} where they apply).
}
\description{
\tabular{ll}{
   Method \tab Computed by \cr
   \code{tss} \tab \code{vegan::decostand(method = "total")}: proportions \cr
   \code{css} \tab \code{metagenomeSeq::cumNorm} with \code{cumNormStatFast}, then \code{MRcounts(norm = TRUE)} \cr
   \code{clr} \tab zeros replaced by \code{pseudocount}, then \code{vegan::decostand(method = "clr")} \cr
   \code{rarefy} \tab \code{vegan::rrarefy} to \code{depth}, samples below it dropped \cr
}
}
\section{The CLR zero replacement}{

\code{vegan::decostand(method = "clr", pseudocount = )} adds the pseudocount to every
count, which makes the result change when a sample's depth is scaled. Here only zeros
are replaced, then the CLR is taken with no pseudocount, so a sample with no zeros is
unaffected by its depth.
}

