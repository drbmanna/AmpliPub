% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxa_compose.R
\name{ap_collapse}
\alias{ap_collapse}
\title{Collapse a table to a taxonomic rank}
\usage{
ap_collapse(x, rank = "genus", drop_unassigned = FALSE)
}
\arguments{
\item{x}{A \code{TreeSummarizedExperiment} from \code{\link[=ap_import]{ap_import()}} with taxonomy.}

\item{rank}{Rank to collapse to.}

\item{drop_unassigned}{Remove features with no assignment at any rank.
Default \code{FALSE}.}
}
\value{
A \code{TreeSummarizedExperiment} with one row per taxon. The tree is
dropped, since a collapsed taxon has no single place on it.
}
\description{
Sums counts across features sharing a label at \code{rank}. Features unassigned
at that rank fall back up the hierarchy and keep a label that says so (see
\code{\link[=ap_taxon_label]{ap_taxon_label()}}), rather than being dropped.
}
\details{
Dropping them is the common alternative and it is not neutral. On this
dataset 91 of 654 features have no genus, carrying real abundance; removing
them would raise every reported genus proportion without saying so.
}
