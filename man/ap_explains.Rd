% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explains.R
\name{ap_explains}
\alias{ap_explains}
\title{Which terms explain the data when they compete in one model}
\usage{
ap_explains(
  beta = NULL,
  alpha = NULL,
  terms,
  beta_metrics = NULL,
  alpha_metrics = NULL,
  permutations = 999L,
  seed = 1L
)
}
\arguments{
\item{beta}{Optional \code{ap_beta} object from \code{\link[=ap_beta]{ap_beta()}}.}

\item{alpha}{Optional \code{ap_alpha} object from \code{\link[=ap_alpha]{ap_alpha()}}. At least one of
\code{beta} and \code{alpha} is required.}

\item{terms}{Character vector of at least two metadata variables, or a
one-sided formula such as \code{~ Site + dx}.}

\item{beta_metrics}{Distances to use. Defaults to everything in \code{beta}.}

\item{alpha_metrics}{Alpha metrics to use. Defaults to everything in \code{alpha}.}

\item{permutations}{Permutations for the PERMANOVA and dispersion tests.
Default \code{999}.}

\item{seed}{Random seed, recorded with the result.}
}
\value{
An object of class \code{ap_explains}: a list with \code{beta} and \code{alpha}
components, each holding \code{terms} (one row per metric per term) and \code{model}
(one row per metric, with \code{model_R2}, \code{sum_marginal_R2} and \code{shared_R2}).
}
\description{
Fits every term together and reports, for each, the variance it explains
after all the others (marginal sums of squares), the variance explained by
the model as a whole, and the part of it no single term can claim.
}
\section{Shared variance}{

\verb{shared_R2 = model_R2 - sum(marginal R2)}. Positive values are variance the
terms explain jointly and cannot be assigned to any one of them, which is
what overlapping or confounded terms produce. Values near zero mean the terms
are close to independent. Negative values mean the terms suppress each other,
each explaining more once the other is in the model.
}

\section{Not a screen}{

The terms here should be chosen before fitting. If they came out of
\code{\link[=ap_screen]{ap_screen()}}, the methods section should say so.
}

