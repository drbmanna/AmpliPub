% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxonomy.R
\name{ap_parse_taxonomy}
\alias{ap_parse_taxonomy}
\title{Split QIIME-style taxonomy strings into rank columns}
\usage{
ap_parse_taxonomy(taxa, ranks = ap_rank_names())
}
\arguments{
\item{taxa}{Character vector of taxonomy strings.}

\item{ranks}{Ranks to return. Defaults to domain through species.}
}
\value{
A data frame with one column per rank, one row per input string.
}
\description{
Accepts the semicolon-delimited, prefixed strings QIIME 2 classifiers emit
(\verb{d__Bacteria; p__Bacteroidota; ...}). Ranks are assigned by their prefix,
never by position, so truncated strings and non-standard domain prefixes
(\code{k__} from Greengenes 13_8, \code{Unassigned}) land in the right column or in
none at all.
}
\details{
Ranks not present in the string, and placeholders that carry no name
(\code{g__}, \code{g__uncultured}, \code{unidentified}), become \code{NA}. An empty rank and a
rank named "uncultured" are the same statement: not identified. Keeping them
apart lets an \code{NA}-aware aggregation quietly treat "uncultured" as a taxon
and pool unrelated organisms under one bar.
}
