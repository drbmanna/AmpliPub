% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxonomy.R
\name{ap_taxon_label}
\alias{ap_taxon_label}
\title{Build a display label for each feature at a given rank}
\usage{
ap_taxon_label(tax, rank = "genus", mark_fallback = TRUE)
}
\arguments{
\item{tax}{A data frame of rank columns, as returned by \code{\link[=ap_parse_taxonomy]{ap_parse_taxonomy()}}.}

\item{rank}{Target rank.}

\item{mark_fallback}{Annotate labels that fell back. Default \code{TRUE}.}
}
\value{
A character vector, one label per row of \code{tax}.
}
\description{
Returns the name at \code{rank}, falling back up the hierarchy when that rank is
unassigned, so a feature classified only to family reads
\verb{f__Lachnospiraceae (unassigned genus)} rather than \code{NA}. Features with no
assignment at any rank return \code{"Unassigned"}.
}
\details{
The fallback is marked in the label on purpose. A bar chart that silently
prints the family name in a genus plot is a chart that overstates its own
resolution.
}
