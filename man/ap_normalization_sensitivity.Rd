% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{ap_normalization_sensitivity}
\alias{ap_normalization_sensitivity}
\title{Does a conclusion survive a change of normalization?}
\usage{
ap_normalization_sensitivity(
  x,
  terms,
  methods = c("tss", "css", "clr", "rarefy"),
  depth = NULL,
  pseudocount = 0.5,
  permutations = 999L,
  seed = 1L
)
}
\arguments{
\item{x}{A \code{TreeSummarizedExperiment} from \code{\link[=ap_import]{ap_import()}}.}

\item{terms}{Metadata variables to test, as for \code{\link[=ap_permanova]{ap_permanova()}}.}

\item{methods}{Normalizations to compare. Default all four.}

\item{depth}{Rarefaction depth for \code{"rarefy"}.}

\item{pseudocount}{Zero replacement for \code{"clr"}.}

\item{permutations}{Permutations for each PERMANOVA. Default \code{999}.}

\item{seed}{Random seed, recorded with the result.}
}
\value{
An object of class \code{ap_normalization_sensitivity}: \code{results} (one row per
normalization per term) and \code{agreement} (one row per term, with the verdicts and
whether they are all the same).
}
\description{
Normalizes the table each way, computes a distance suited to that normalization,
runs \code{\link[=ap_permanova]{ap_permanova()}} with its dispersion check for each term, and reports whether the
verdict for each term is the same under every normalization.
}
\details{
\tabular{ll}{
   Normalization \tab Distance \cr
   \code{tss}, \code{css}, \code{rarefy} \tab Bray-Curtis, \code{vegan::vegdist(method = "bray")} \cr
   \code{clr} \tab Euclidean on the CLR, which is Aitchison distance \cr
}
}
