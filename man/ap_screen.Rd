% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen.R
\name{ap_screen}
\alias{ap_screen}
\title{Screen every metadata variable against alpha and beta diversity}
\usage{
ap_screen(
  alpha = NULL,
  beta = NULL,
  variables = NULL,
  permutations = 999L,
  n_resample = 100L,
  fraction = 0.8,
  top_k = 5L,
  seed = 1L,
  max_levels = 20L
)
}
\arguments{
\item{alpha}{Optional \code{ap_alpha} object from \code{\link[=ap_alpha]{ap_alpha()}}.}

\item{beta}{Optional \code{ap_beta} object from \code{\link[=ap_beta]{ap_beta()}}. At least one of
\code{alpha} and \code{beta} is required.}

\item{variables}{Metadata variables to screen. \code{NULL} (default) screens every
usable variable. Named variables still pass through the same guards.}

\item{permutations}{Permutations for each PERMANOVA and dispersion test.
Default \code{999}.}

\item{n_resample}{Subsamples for the stability estimate. Default \code{100},
minimum \code{10}.}

\item{fraction}{Share of samples in each subsample, strictly between 0 and
\enumerate{
\item Default \code{0.8}.
}}

\item{top_k}{A row is stable in a subsample when it ranks in the top \code{top_k}.
Default \code{5}.}

\item{seed}{Random seed for the permutations and the subsamples, recorded
with the result.}

\item{max_levels}{Passed to \code{\link[=ap_scan_metadata]{ap_scan_metadata()}}. Default \code{20}.}
}
\value{
An object of class \code{ap_screen}: a list with \code{results} (one row per
test, ranked by effect), \code{by_variable}, \code{skipped}, \code{untestable},
\code{n_tests}, \code{expected_false_positives}, \code{resamples}, and the settings used.
}
\description{
Tests each usable metadata variable against every alpha diversity metric and
every beta diversity distance, ranks the results by effect size, and reports
how stable each rank is when the samples are subsampled.
}
\section{Ranking}{

Rows are ordered by variance explained adjusted for degrees of freedom,
never by p. Raw R2 is biased upward: with no effect at all its expectation is
about \code{df / (n - 1)}, so a variable with many levels, or one recorded on few
samples, outranks a real two-group effect on the raw scale. Beta rows are
therefore ranked on adjusted PERMANOVA R2, \code{1 - (1 - R2)(n - 1)/(n - df - 1)},
and numeric alpha rows on the same adjustment of Spearman rho squared.
Categorical alpha rows use eta squared based on the Kruskal-Wallis H,
\code{(H - k + 1) / (n - k)}, from \code{rstatix::kruskal_effsize}, which already
subtracts the null expectation of H. Raw values stay in \code{effect}, adjusted
ones in \code{effect_adj}.

These are all proportions of variance, but not of the same variance: R2
partitions distance sums of squares and eta squared (H) partitions rank
variance. Order within a family is exact; order across families is
approximate.
}

\section{Multiple testing}{

One Benjamini-Hochberg correction is applied across every test the screen
ran, alpha and beta together. The number of tests is part of the result and
is printed with it.
}

\section{Stability}{

\code{n_resample} subsamples of \code{fraction} of the samples are drawn without
replacement, and every effect size is recomputed on each. Stability is the
share of subsamples in which a row ranks in the top \code{top_k}. Subsampling is
used rather than the bootstrap because resampling with replacement puts
duplicate samples at zero distance from each other, which inflates
PERMANOVA R2. Subsamples overlap, so stability says how much a rank depends
on which samples were drawn. It does not say whether the effect is real.
}

\section{Variables that are not screened}{

Constant variables, identifiers, variables with more than \code{max_levels}
levels, repeated-measures identifiers (which need a mixed model, not a
screen), and categorical variables whose smallest level has fewer than 3
samples. Each is listed in \code{skipped} with the reason.
}

