% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permanova.R
\name{ap_permanova}
\alias{ap_permanova}
\title{PERMANOVA with a bound dispersion check}
\usage{
ap_permanova(
  beta,
  terms,
  metrics = NULL,
  permutations = 999L,
  by = c("margin", "terms"),
  strata = NULL,
  seed = 1L
)
}
\arguments{
\item{beta}{An \code{ap_beta} object from \code{\link[=ap_beta]{ap_beta()}}.}

\item{terms}{Character vector of metadata variables, or a one-sided formula
such as \code{~ Site + dx}. Multiple terms are adjusted for one another.}

\item{metrics}{Metrics to test. Defaults to everything in \code{beta}.}

\item{permutations}{Number of permutations. Default \code{999}.}

\item{by}{\code{"margin"} (default) assesses each term after all others, so the
formula order does not matter. \code{"terms"} is sequential and order-dependent.}

\item{strata}{Optional metadata variable to permute within, for a blocked
design.}

\item{seed}{Random seed, recorded with the result.}
}
\value{
An object of class \code{ap_permanova}: a list with \code{results} (one row per
metric per term), \code{dispersion} (one row per metric per categorical term),
and the settings used.
}
\description{
Runs \code{vegan::adonis2} across every distance metric in an \code{ap_beta} object,
and \code{vegan::betadisper} with \code{vegan::permutest} for each categorical term,
then states what the combination means.
}
