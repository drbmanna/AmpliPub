% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qza.R
\name{ap_read_qza}
\alias{ap_read_qza}
\title{Read a QIIME 2 artifact into an R object}
\usage{
ap_read_qza(path, tmpdir = NULL)
}
\arguments{
\item{path}{Path to a \code{.qza} file.}

\item{tmpdir}{Directory to extract into. Defaults to a fresh session temp
directory, removed on exit.}
}
\value{
The object described above, with attributes \code{qza_uuid}, \code{qza_type},
\code{qza_format}, \code{qza_framework}.
}
\description{
Reads the payload of a \code{.qza} and returns the natural R representation for
its semantic type. The QIIME 2 framework is not required; the artifact is a
zip archive holding data in standard formats.
}
\details{
Supported semantic types:\tabular{ll}{
   Semantic type \tab Returns \cr
   \verb{FeatureTable[*]} \tab integer or numeric matrix, features x samples \cr
   \code{Phylogeny[Rooted]}, \code{Phylogeny[Unrooted]} \tab \code{ape::phylo} \cr
   \code{FeatureData[Taxonomy]} \tab data frame: \code{feature_id}, \code{taxon}, \code{confidence} \cr
   \code{FeatureData[Sequence]} \tab \code{Biostrings::DNAStringSet} \cr
   \code{SampleData[AlphaDiversity]} \tab named numeric vector \cr
   \code{DistanceMatrix} \tab \code{stats::dist} \cr
   \code{PCoAResults} \tab list: \code{vectors}, \code{eigvals}, \code{prop_explained} \cr
}


Every returned object carries the artifact's \code{uuid}, \code{type}, \code{format} and
framework version as attributes, so provenance survives the read.
}
