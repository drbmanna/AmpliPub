% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{ap_import}
\alias{ap_import}
\title{Import an amplicon dataset}
\usage{
ap_import(
  table,
  metadata,
  tree = NULL,
  taxonomy = NULL,
  features_are_rows = TRUE,
  expect_counts = TRUE,
  max_drop = 0.1,
  drop_empty_features = TRUE
)
}
\arguments{
\item{table}{Feature table: path to a \code{.qza}, \code{.biom} or \code{.tsv}, or a numeric
matrix with features as rows.}

\item{metadata}{Sample metadata: path to a TSV/CSV whose first column holds
sample IDs, or a data frame with sample IDs in the row names or first column.}

\item{tree}{Optional phylogeny: path to a \code{.qza} or Newick file, or an
\code{ape::phylo}. Required for UniFrac and Faith's PD.}

\item{taxonomy}{Optional taxonomy: path to a \code{.qza} or TSV, or a data frame
with \code{feature_id} and \code{taxon} columns.}

\item{features_are_rows}{Orientation of a TSV or matrix input. Default \code{TRUE}.}

\item{expect_counts}{Warn when the table is not integer counts. Default \code{TRUE}.}

\item{max_drop}{Largest fraction of table samples allowed to lack a metadata
row before import fails. Default \code{0.1}.}

\item{drop_empty_features}{Remove features with zero counts everywhere, which
is normal after subsetting. Default \code{TRUE}.}
}
\value{
A \code{TreeSummarizedExperiment} with a \code{counts} assay.
}
\description{
The entry point. Takes a feature table and sample metadata, optionally a
phylogeny and taxonomy, runs the join and consistency guards, and returns a
\code{TreeSummarizedExperiment} carrying an AmpliPub provenance log.
}
\details{
AmpliPub is pipeline-agnostic. Anything that yields a feature table works:
QIIME 2 \code{.qza}, BIOM (v1 JSON or v2 HDF5), a plain TSV, or an in-memory
matrix. QIIME 2 artifacts are read directly, without QIIME 2 installed.

The returned object is a plain \code{TreeSummarizedExperiment}, not a wrapper
class, so \code{mia}, \code{ANCOMBC} and the rest of the Bioconductor stack accept it
as-is. The provenance log lives in its \code{metadata()}; see \code{\link[=ap_summary]{ap_summary()}}.
}
\section{Sample IDs are read as text}{

Metadata is read with every column as character, then types are inferred
column by column, because sample IDs are the one column that must never be
guessed. Baxter 2016 IDs look like \code{2003650}. Read as numbers they survive;
an ID like \code{007} does not. A silently reformatted ID does not fail the join
loudly, it fails it quietly for the subset that happened to look numeric.
}

\examples{
set.seed(1)
counts <- matrix(rpois(200, 20), nrow = 20,
                 dimnames = list(paste0("ASV", 1:20), paste0("S", 1:10)))
meta <- data.frame(group = rep(c("a", "b"), each = 5),
                   row.names = paste0("S", 1:10))
x <- ap_import(counts, meta)
ap_summary(x)
}
