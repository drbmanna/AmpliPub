% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_alpha.R
\name{ap_plot_alpha}
\alias{ap_plot_alpha}
\title{Plot alpha diversity by group}
\usage{
ap_plot_alpha(
  alpha,
  group,
  test = NULL,
  type = c("box", "violin"),
  points = TRUE,
  metrics = NULL,
  annotate = TRUE,
  ncol = NULL
)
}
\arguments{
\item{alpha}{An \code{ap_alpha} object from \code{\link[=ap_alpha]{ap_alpha()}}.}

\item{group}{Metadata variable to split by.}

\item{test}{Optional \code{ap_alpha_test} result to annotate with. When \code{NULL}
(default) and \code{annotate = TRUE}, \code{\link[=ap_alpha_test]{ap_alpha_test()}} is run for you.}

\item{type}{\code{"box"} (default) or \code{"violin"}.}

\item{points}{Draw the individual samples. Default \code{TRUE}.}

\item{metrics}{Metrics to include. Defaults to everything in \code{alpha}.}

\item{annotate}{Write the test result onto each panel. Default \code{TRUE}.}

\item{ncol}{Facet columns. \code{NULL} lets ggplot2 decide.}
}
\value{
A ggplot object.
}
\description{
One panel per metric, with the test result and effect size written onto each
panel rather than left to the caption. A reviewer should be able to read the
claim and its strength off the figure.
}
\details{
The effect size is shown alongside the p-value on purpose. A boxplot with
\code{p < 0.001} and nothing else invites the reader to assume a large difference,
which at n in the hundreds it usually is not.
}
