% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_figure.R
\name{ap_save_figure}
\alias{ap_save_figure}
\title{Save a publication figure}
\usage{
ap_save_figure(
  plot,
  name,
  dir,
  width = NULL,
  height = NULL,
  legend = NULL,
  dpi = 600
)
}
\arguments{
\item{plot}{A ggplot object.}

\item{name}{File name without extension.}

\item{dir}{Output directory, created if missing.}

\item{width}{\code{"single"} (89 mm), \code{"onehalf"} (120 mm), \code{"double"} (183 mm),
or a number of millimetres. Defaults to the size the \verb{ap_plot_*} function
laid the figure out for.}

\item{height}{Height in millimetres, at most 247. Default as for \code{width}.}

\item{legend}{Character vector for the legend file. \code{NULL} writes none.}

\item{dpi}{PNG resolution. Default \code{600}.}
}
\value{
The paths written, invisibly.
}
\description{
Writes the figure as PDF (vector, fonts embedded), SVG (vector, text left
editable) and PNG, at a journal column width, plus \verb{<name>_legend.txt}
holding the statistics and caveats that the publication figure leaves off
the panel. A figure is never exported without the caveats its statistic came
with; they move from the panel to the legend.
}
\details{
In the SVG the font is written as \verb{Arial, "Liberation Sans", sans-serif}, so
it shows in Arial wherever Arial is installed. The PDF embeds whatever font
drew it, which on a machine without Arial is its metric-compatible
substitute.
}
