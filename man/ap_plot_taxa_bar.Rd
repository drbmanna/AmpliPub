% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_taxa.R
\name{ap_plot_taxa_bar}
\alias{ap_plot_taxa_bar}
\title{Stacked composition bar chart}
\usage{
ap_plot_taxa_bar(
  x,
  rank = "genus",
  n = 15L,
  group = NULL,
  mode = c("sample", "group"),
  order_by = NULL
)
}
\arguments{
\item{x}{A \code{TreeSummarizedExperiment} from \code{\link[=ap_import]{ap_import()}}, or a data frame
from \code{\link[=ap_top_taxa]{ap_top_taxa()}}.}

\item{rank}{Rank to collapse to. Default \code{"genus"}.}

\item{n}{Number of taxa to show. Default \code{15}.}

\item{group}{Optional metadata variable to facet by.}

\item{mode}{\code{"sample"} (default) draws one bar per sample; \code{"group"} draws
the mean composition per group.}

\item{order_by}{Taxon whose abundance orders the samples within each facet.
Defaults to the most abundant. Ignored when \code{mode = "group"}.}
}
\value{
A ggplot object.
}
\description{
Relative abundance of the most abundant taxa, one bar per sample or one per
group.
}
\details{
The caption states how many taxa were pooled into \code{Other} and how much
abundance they carry. A stacked bar chart showing 15 genera out of 250 looks
like a complete picture, and whether it is depends entirely on how large
\code{Other} is.
}
