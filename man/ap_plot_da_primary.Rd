% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_da.R
\name{ap_plot_da_primary}
\alias{ap_plot_da_primary}
\title{Main-text differential abundance figure from one primary method}
\usage{
ap_plot_da_primary(
  concordance,
  primary = "ancombc2",
  type = c("effect", "abundance"),
  x = NULL,
  max_features = 25L,
  features = NULL,
  pub = ap_pub_options()
)
}
\arguments{
\item{concordance}{An \code{ap_da_concordance} result from \code{\link[=ap_da_concordance]{ap_da_concordance()}}.}

\item{primary}{The method whose calls are drawn. Default \code{"ancombc2"}.}

\item{type}{\code{"effect"} (default) or \code{"abundance"}.}

\item{x}{For \code{type = "abundance"}: the TreeSummarizedExperiment \code{\link[=ap_da]{ap_da()}} ran on.}

\item{max_features}{Cap on rows in each direction, largest effects first. Default
\code{25}: at most 25 enriched and 25 depleted features are drawn. Every call is in the
results table, and the legend says how many are not shown.}

\item{features}{Feature IDs to draw, from the primary method's calls. Required for
\code{type = "abundance"}, at most 8; optional for \code{type = "effect"}.}

\item{pub}{\code{\link[=ap_pub_options]{ap_pub_options()}}.}
}
\value{
A ggplot object.
}
\description{
The features one method called, as that method's effect with its 95\% interval
(\code{type = "effect"}, the main-text figure): enriched features above depleted, each block
sorted by effect. Agreement between methods is not repeated here; it is the concordance
figure's job (\code{\link[=ap_plot_concordance]{ap_plot_concordance()}}).
}
\details{
\code{type = "abundance"} draws the per-sample relative abundance behind a few chosen calls
(\code{features}), in the same row order. It is for the handful of taxa a paper discusses in
the text: with dozens of rows it cannot be read, and for rare taxa most samples are zero,
so the detection count printed beside each group carries more than the box.

The primary method is meant to be chosen before the results are seen (the workflow's
\code{da_primary}). Chosen afterwards, from whichever method called the most, it is the
selection the other methods are there to guard against.

ALDEx2 cannot be the primary method for \code{type = "effect"}: the column AmpliPub stores as
its standard error is \code{diff.win}, a within-condition dispersion, and an interval built from
it would not be a confidence interval.
}
